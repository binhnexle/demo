import { createSlice } from "@reduxjs/toolkit";

const slice = createSlice({
  name: "avatar",
  initialState: {
    id: 1,
    avatar: "avatar.jpg",
  },

  reducers: {},
});
export default slice;
